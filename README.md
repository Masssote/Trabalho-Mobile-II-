# Trabalho-Mobile-II-


Grupo: João Pedro Lopes massote

Como rodar o backend:

bash
cd backend-mobile-ii
npm install
node index.js

Como rodar o app:

bash
cd crud_app
flutter pub get
flutter run -d chrome
